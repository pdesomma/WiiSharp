namespace WiiSharp;

/// <summary>
/// Region code stored in the disc's region area.
/// </summary>
public enum DiscRegion : uint
{
    Japan = 0,
    UnitedStates = 1,
    Europe = 2,
    Korea = 4,
}
